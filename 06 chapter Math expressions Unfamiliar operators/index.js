num++;
// This is a short way of writing...
var num = num + 1;
// It increments the variable by 1.
// for decrement
num--;
// You can use these expressions in an assignment. But the result may surprise you
var number1 = 1;
var newnum = number1++
console.log(newnum)
// it prints the value 1 because the increment is after the variable 
var number2 = 1;
var numAddition = ++number2;
console.log(numAddition) 
// it prints the value 2 beacuse the increment is before the variable
var number3 = 1;
var newnum2 = number3--
console.log(newnum2)
// it prints the value 1 because the decrement is after the variable 
var number4 = 1;
var numSubtraction = --number4;
console.log(numSubtraction) 
// it prints the value 0 beacuse the increment is before the variable

